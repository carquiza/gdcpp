# gdcpp — TODO

## Template Validation

- [ ] Debug build verified on Linux (.so loads in Godot editor, SpinningCube runs)
- [ ] Debug build verified on Windows (.dll loads in Godot editor, SpinningCube runs)
- [ ] Release (module) build verified on Linux (single ELF binary, no editor)
- [ ] Release (module) build verified on Windows (single .exe, no editor)
- [ ] Web build verified on Linux (Emscripten, .wasm + .js runs in browser)
